{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -Eq 'rm -rf|git push .*--force|git push .*main'; then echo 'Bloqueado: comando perigoso ou push direto para main.' >&2; exit 1; fi"
          }
        ]
      }
    ]
  }
}
