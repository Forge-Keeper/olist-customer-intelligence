{
  "jobs": {
    "ibge_municipality_gdp": {
      "arguments": [
        "--target-table",
        "${target}.bronze.ibge_municipality_gdp",
        "--execution-runs-table",
        "${target}_admin.operations.execution_runs",
        "--quality-results-table",
        "${target}_admin.quality.data_quality_results",
        "--periods",
        "2018"
      ]
    },
    "ibge_municipality_business_activity": {
      "arguments": [
        "--target-table",
        "${target}.bronze.ibge_municipality_business_activity",
        "--periods",
        "2018"
      ]
    }
  }
}
